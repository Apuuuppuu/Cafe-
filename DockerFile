# Use an official PHP image from Docker Hub
FROM php:8.1-apache

# Enable the Apache mod_rewrite module (if needed)
RUN a2enmod rewrite

# Copy your project files into the container's web root directory
COPY . /var/www/html/

# Set proper permissions for files and directories
RUN chown -R www-data:www-data /var/www/html/

# Expose the default Apache port
EXPOSE 80

# Start Apache in the foreground
CMD ["apache2-foreground"]
